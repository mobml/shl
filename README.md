# shl

A straightfoward new shell written in Rust.

## Install 
Clone the repo:
```bash
git clone https://github.com/mobml/shl

```
Change to directory:
```bash
cd shl
```
Run:
```bash
cargo run 
```
Compile: 
```bash
cargo build 
```
